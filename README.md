# .github
LDPlayer Instance List and LDPlayer Display Session keep a guest you operate on a host you own. LDPlayer Engine Profile is a profile you named. Keep maps honest, media folders few, do not add a package you have no right to run, and date the instance after the job. Keep the host named.
